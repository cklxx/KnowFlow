import { Screen } from '@/ui/components/Screen';
import { Text } from '@/ui/components/Text';

export default function SearchScreen() {
  return (
    <Screen>
      <Text variant="title">Search</Text>
      <Text>
        Hybrid keyword and semantic search hub. Wire up to SQLite/Tantivy indices and card detail
        pages during the data integration milestone.
      </Text>
    </Screen>
  );
}
