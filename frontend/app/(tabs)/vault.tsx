import { Screen } from '@/ui/components/Screen';
import { Text } from '@/ui/components/Text';

export default function VaultScreen() {
  return (
    <Screen>
      <Text variant="title">Vault</Text>
      <Text>
        Browse layered knowledge sources (highlights, annotations, cards, evergreen notes).
        Implement filters and list virtualization in upcoming milestones.
      </Text>
    </Screen>
  );
}
