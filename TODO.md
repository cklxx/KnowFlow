# TODO 清单

## Backend
- [ ] 「老婆模式」管理接口：暴露 CRUD API（标签、人工标记、优先级），并将配置与 `digest_items` 关联，满足产品蓝图 12.1.4 的个性化要求。
- [ ] RSS/新闻抓取持久化：为 `backend/src/services/digest.rs` 引入数据库模型与增量更新策略，支持历史归档与命中缓存命中率统计。
- [ ] 「大家别慌」事实核查管线：新增热点收集、校验模板和 `GET /api/digest/calm` 输出，覆盖产品文档第 5.3 节的专栏需求。
- [ ] 反馈采集 API：实现“听懂了吗”打分与主观反馈写入（含兴趣标签回传），为后续个性化与模型调优提供数据闭环。

## Frontend
- [ ] 历史日期切换：在首页提供日期选择/左右切换，调用 `GET /api/digest/:date`（待后端支持）浏览往期内容。
- [ ] 「听懂了吗」反馈入口：在摘要卡片增加听懂/有点难/和我没关系按钮，并对接新反馈 API。
- [x] 文字稿内嵌展开：在页面内直接展开/折叠文字稿，兼容移动端阅读与可访问性，而非仅跳转外链。
- [ ] 「大家别慌」栏目前端：新增 Tab 或独立页面呈现事实校验内容，复用音频 + 文字双通道体验。
- [ ] 「老公注释」展示：当后台写入注释或语音备忘时，前端需要可视化标记并支持播放文本/音频。

## Testing & DevOps
- [ ] 将 `npm run test:e2e` 接入 CI/CD 流水线，确保 PR 自动跑通端到端脚本。
- [ ] 引入视觉回归（Playwright + screenshot baseline）捕捉摘要 UI 的非预期变动。
- [ ] 为老婆模式/事实核查 API 添加后端集成测试夹具，覆盖无密钥、离线以及定制标签场景。
- [ ] 监控告警：增加 Playwright + Rust 服务健康的基础监控脚本，并在失败时发送通知（可先接入 Slack/Webhook）。
